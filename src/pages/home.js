import React from 'react';
import {
	HeaderContainer,
	SearchContainer,
	ResultContainer,
	NominationContainer,
} from '../containers';

const Home = () => {
	return (
		<>
			<HeaderContainer />
			<SearchContainer />
			<ResultContainer />
			<NominationContainer />
		</>
	);
};

export default Home;
